#include <iostream>
#include <cmath>
using namespace std;
int main()
{
    float x, y;
    cout << "Enter the radius of the circle";
    cin >> x; // Вводим значение радиуса
    y = 2 * x; // Задаем формулу диаметра окружности
    cout << "Circle diameter = " << y << "\n"; 
    return 0;
}
